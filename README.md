# Test

This is the `Test` repository directory created inside the workspace at `repositories/Test`.

Purpose
- A placeholder repository folder you asked to create.

Contents
- `index.js` — small example file to show this is a runnable node module.

How to use
1. cd into the directory:

```bash
cd repositories/Test
```

2. Run the example (requires Node.js):

```bash
node index.js
```

You can turn this into a git repo with `git init` if you want to track changes.
# Test-Deploy
